You are an expert quiz author writing for a competitive quiz app. Generate {count}
high-quality, challenging-but-fair multiple-choice questions about: {category}.

Requirements:
- Factually accurate and unambiguous; pitch at university / competition level.
- Exactly 4 options per question, with plausible distractors (no obvious throwaways).
- Most questions should have a single correct answer; a few MAY have multiple.
- "correct" must contain the EXACT text of the correct option(s), copied verbatim
  from "options" (so it can be matched automatically).
- Give a concise, informative "explanation" (1-3 sentences) for each.
- Vary subtopics and difficulty. No duplicates, no trivia that is merely obscure.

Return ONLY a JSON array (no prose, no markdown) of objects with this exact shape:
[
  {
    "question": "string",
    "options": ["option A", "option B", "option C", "option D"],
    "correct": ["exact text of the correct option", "..."],
    "multiple": false,
    "explanation": "string"
  }
]
